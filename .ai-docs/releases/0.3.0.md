# Schema Engine M19 coordinated Experimental candidate release notes

- **State:** Reviewed dirty-tree pre-commit candidates; not publishable and not
  selected from a clean commit
- **Date:** 2026-07-19
- **Plan state:** PLAN-021 checkpoints 1–3 complete; checkpoint 4 private Git
  and clean rebuild require separate authorization
- **Recommended channel after publication:** `next`; exact versions remain the
  audit-safe installation form
- **License:** `AGPL-3.0-only` or a separate paid commercial agreement
- **API state:** Public + Experimental + Active; no Stable API or support SLA
- **Repository/provenance:** private repository, no advertised repository URL,
  trusted publisher or npm provenance

## Exact candidate line

| Package                                 | Candidate | Required Schema Engine peer |
| --------------------------------------- | --------- | --------------------------- |
| `@rabassoft/schema-engine`              | `0.3.0`   | none                        |
| `@rabassoft/schema-engine-angular`      | `0.3.0`   | core `^0.3.0`               |
| `@rabassoft/schema-engine-angular-aria` | `0.1.0`   | base Angular `^0.3.0`       |

These are independent SemVer lines. Equal core/base versions reflect their
actual M18 Public changes and do not create lockstep versioning. The pilot
starts independently at `0.1.0`.

Checkpoint 3 generated and reviewed deterministic dirty-tree candidates only.
Their ignored evidence records exact bytes, hashes and `sourceCommit: null`;
they are comparison inputs for checkpoint 4, not selected publication hashes.
No source commit exists yet. This document does not report registry
availability.

## Included capability

M19 packages only completed M18/SPEC-008:

- root-only static sections, tabs, accordions and bounded logical grid in the
  framework-neutral normalized presentation forest;
- descriptor-safe compilation, closed diagnostics, exact identity, immutable
  normalized definitions and atomic default-forest fallback;
- target-owned tab/accordion state, mounted hidden descendants, source-order
  grid fallback and unchanged application-owned value/baseline/runtime state;
- independent native Angular and private Standard projection;
- a Public Experimental Angular presentation-container SPI with mandatory
  native section/tabs/accordion/grid registrations; and
- one isolated Angular Aria 22 pilot with explicit style import and no field
  renderers or runtime ownership.

No runtime snapshot, method, operation, validation, scope, persistence or data
authority contract changes in this release.

## Public migration from `0.2.0`

### Core additions

The root package adds these Public Experimental contracts:

- raw UI Schema: `UiTabsSchema`, `UiAccordionSchema`,
  `UiPresentationPanelSchema`, `UiGridSchema` and `UiGridItemSchema`;
- normalized presentation: `PresentationTabsDefinition`,
  `PresentationAccordionDefinition`, `PresentationPanelDefinition`,
  `PresentationGridDefinition` and `PresentationGridItemDefinition`; and
- localized labels: `AdvancedPresentationTextMember`,
  `AdvancedPresentationLabelDefinition` and
  `AdvancedPresentationTextResolutionContext`.

`UiPresentationEntry`, `PresentationEntryDefinition` and
`TextResolutionContext` widen only for those accepted root presentation forms.
Compiler diagnostics/fallback and manual-definition validation widen
accordingly. Existing sections remain source compatible.

### Base Angular additions

The base adapter adds exactly:

- `AngularPresentationContainerDefinition`;
- `AngularPresentationContainerRenderModel`;
- `AngularPresentationContainerRenderer`;
- `AngularPresentationContainerRendererType`;
- `AngularPresentationContainerTester`;
- `AngularPresentationContainerRegistration`;
- `SchemaPresentationEntryOutletComponent`;
- `SchemaPresentationPanelOutletComponent`; and
- `provideSchemaPresentationContainer()`.

`provideSchemaEngineAngular()` keeps its signature and installs the internal
resolver plus four mandatory native registrations. `SchemaFormDirective`
projects the widened forest. Provider tokens, resolver classes, host factories,
native components and claim contexts remain Internal.

### Angular Aria pilot

The first pilot package exports only
`provideSchemaEngineAngularAriaContainers()` from its root and opt-in
`./styles.css`. It registers exactly section, tabs, accordion and grid
containers at rank `10`; it does not configure the base adapter, fields or
application theme.

The only Public Experimental style properties are:

```css
--se-aria-container-surface
--se-aria-container-text
--se-aria-container-border
--se-aria-container-accent
--se-aria-container-radius
--se-aria-container-gap
```

Applications continue to own light/dark selectors and values. Internal host
classes and selectors are not customization API.

## Compatibility

```text
@rabassoft/schema-engine-angular ^0.3.0
@angular/core >=22.0.6 <23.0.0
@angular/forms >=22.0.6 <23.0.0
@rabassoft/schema-engine-angular-aria 0.1.x
@angular/aria >=22.0.5 <23.0.0
@angular/cdk >=22.0.5 <23.0.0
```

| Package line         | Schema Engine peer    | Framework/UI peers                                           | Verified endpoints                           |
| -------------------- | --------------------- | ------------------------------------------------------------ | -------------------------------------------- |
| Core `0.3.x`         | none                  | none                                                         | package/source/consumer evidence             |
| Angular `0.3.x`      | core `^0.3.0`         | Angular core/forms `>=22.0.6 <23.0.0`, exact aligned patch   | `22.0.6` and `22.0.7`                        |
| Angular Aria `0.1.x` | base Angular `^0.3.0` | Angular core `>=22.0.6 <23.0.0`; Aria/CDK `>=22.0.5 <23.0.0` | Angular `22.0.6`/`22.0.7`, Aria/CDK `22.0.5` |

The resolved CDK patch must equal the exact peer required by the resolved Aria
patch. Angular 23, prerelease/deprecated/misaligned Angular packages, another UI
kit and legacy Angular are unsupported by this candidate.

## Installation states

Before registry publication, only the reviewed dirty-tree tarballs produced by
completed checkpoint 3 represent local M19 comparison inputs. Existing npm
`next`, `latest` and unqualified installs continue to resolve observed
published state, currently the immutable core/base `0.2.0` line; the pilot is
not yet public.

After the separately approved package publications, intended audit forms are:

```sh
npm install @rabassoft/schema-engine@0.3.0
npm install @rabassoft/schema-engine-angular@0.3.0
npm install @rabassoft/schema-engine-angular-aria@0.1.0
```

The dependency-first `next` sequence is core, base Angular, then pilot. No
coordinated consumer evidence is accepted from a partial `next` state.

The pilot's first publication may or may not expose `latest`; the release must
observe it. If absent, adding it requires a separate later approval. Established
base/core `latest` aliases move only after all exact/`next` checks, base first
and core second. No evidence is accepted from that mixed window. Any alias is
registry routing only and never implies Stable.

## Angular composition and styles

Every Angular application installs the base provider directly or through the
native provider used by the complete native form setup. The pilot is optional
and is registered after it:

```ts
import { provideSchemaEngineAngularNative } from '@rabassoft/schema-engine-angular';
import { provideSchemaEngineAngularAriaContainers } from '@rabassoft/schema-engine-angular-aria';

bootstrapApplication(AppComponent, {
  providers: [
    provideSchemaEngineAngularNative(),
    provideSchemaEngineAngularAriaContainers(),
  ],
});
```

Import the pilot baseline explicitly when desired:

```css
@import '@rabassoft/schema-engine-angular-aria/styles.css';
```

Without the pilot, mandatory native containers remain available. Without its
stylesheet, pilot semantics remain functional but no visual baseline is
applied.

## Distribution boundary

Each package independently carries the exact AGPL license/notices, preferred
TypeScript source and frozen package-local build harness. Core/base contain no
pilot, Aria/CDK import, peer, style or asset. The pilot does not bundle or
relicense Angular Aria/CDK and contains no reference application or private
architecture documentation.

The development repository remains private pending D-043 sanitization. No
package advertises its inaccessible URL or claims provenance/trusted
publishing. The public commercial contact is `ricard@rabassoft.com`; no final
commercial terms, contribution policy, public issue tracker or support SLA are
represented.

## Exclusions

This candidate does not add wizards, actions, workflow, conditional/nested/item
layout, arbitrary responsive grids, controlled layout state, SSR/hydration,
field renderers from Angular Aria, generic theme tokens, a Standard package,
React, Vue, another UI kit, Angular 23, legacy Angular, Stable API,
repository publication, Git tag, GitHub Release, OIDC, provenance or release
automation.
