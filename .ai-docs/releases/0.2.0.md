# Schema Engine 0.2.0 coordinated Experimental release notes

- **State:** Published and verified; core and Angular `next`/`latest` are
  coordinated at `0.2.0`
- **Date:** 2026-07-16
- **Channel after publication:** recommended `next`; `latest` moves only after
  both packages pass coordinated live verification
- **License:** `AGPL-3.0-only` or a separate paid commercial agreement
- **API state:** Public + Experimental + Active
- **Core target:** `@rabassoft/schema-engine@0.2.0`
- **Angular target:** `@rabassoft/schema-engine-angular@0.2.0`
- **Source commit:** `ce53dc1f5b3147ddd24e14912c0ff9dc1b32e412`

## Selected candidates

| Package                            |  Bytes | SHA-512                                                                                                                            |
| ---------------------------------- | -----: | ---------------------------------------------------------------------------------------------------------------------------------- |
| `@rabassoft/schema-engine`         | 200245 | `155ae047c8ee949bddcaba412fcff90e4b65396a47f89f63e065e7b7814e8a8e0e2851d8e891465d12f69b54fa00192fe5884b163deb292aedec73f9d13e028a` |
| `@rabassoft/schema-engine-angular` |  93133 | `aa035adb83c01ae1ffccae2126c78f0095ec4f930547d923b80ba7f0419a39ead58dfe45c35818fde4b884dd31793cec17aa2b8c3963520c24f1891d165a5154` |

The clean rebuild matched pre-commit bytes exactly. Hashes were reverified and
both publication rehearsals passed from a fresh neutral path. These candidates
are immutable release inputs; npm publication remains separately gated.

Core and Angular were published and verified on 16 July 2026. Their public
bytes, integrity, registry signatures, AGPL/source boundaries and neutral path
metadata match the selected candidates; neither has repository metadata or
provenance. Both `next` aliases are `0.2.0`; both `latest` aliases intentionally
remain `0.1.0` pending the separately gated coordinated transition.

During the planned minimal transition window, Angular `latest` moved first and
was verified while core `latest` temporarily remained `0.1.0`; no consumer
evidence was accepted from that mixed state.

Core `latest` then moved under its separate approval. Exact, `@next`,
`@latest` and unqualified consumers passed for the coordinated pair, and review
052 closed PLAN-015 with zero findings. The APIs remain Public + Experimental +
Active; neither dist-tag implies Stable.

## Included capability

This coordinated MINOR contains completed M14/SPEC-006 nullable primitive
leaves. The compiler accepts only exact two-member primitive-plus-null type
arrays at supported editable leaves, normalizes the required `nullable`
capability and preserves all scalar, container, identity, enum and general-union
exclusions. Existing definition-aware operations/runtime accept explicit null
only for nullable leaves. Native string, number/integer and boolean renderers
provide the accepted localized set-null intention and confirmed-null status;
string enum remains excluded.

## Source migration from 0.1.0

1. Every manually authored primitive `FieldDefinition` or `FieldTemplate` adds
   required `nullable: boolean`. Use `false` for scalar leaves and `true` only
   for the exact accepted nullable primitive-leaf capability. Definitions
   produced by `compileFormDefinition()` already contain the member.
2. Every manually authored `AngularFieldTextSnapshot` adds required
   `setNullLabel` and `nullValueLabel`. Exhaustive handling of `FieldTextMember`
   adds `set-null` and `null-value`.

No operation shape, expectation, presence/snapshot contract, runtime method,
renderer registration, output, provider, export or entry point changed. Every
affected API remains Experimental.

## Compatibility

| Adapter | Core     | Angular core/forms | Build/lower Angular | Upper tested |
| ------- | -------- | ------------------ | ------------------- | ------------ |
| `0.2.x` | `^0.2.0` | `>=22.0.6 <23.0.0` | `22.0.6`            | `22.0.6`     |

Angular packages must resolve as one exactly aligned tuple. Prerelease,
deprecated or misaligned Angular combinations are unsupported.

## Distribution boundary

Both candidates retain root-only exports, complete package-local preferred
TypeScript source and frozen build harnesses, the exact AGPL license/notices and
no private repository URL. GitHub remains private, so no npm provenance,
trusted publisher or staged workflow is claimed. Candidate preparation uses a
neutral publication path to prevent repeating `0.1.0`'s registry-generated
local username/workspace disclosure.

Live `0.1.0` bytes remain immutable and pre-M14. The verified `0.2.0` bytes now
serve both observed `next` and `latest`; neither routing alias changes their
Public + Experimental + Active classification.

## Exclusions

General unions, standalone null, nullable root/object/array/item/identity,
`enum + null`, coercion, defaults, arrays of primitives, dynamic/external
references, async validation, persistence, new renderers, Stable APIs,
repository publication and D-043 remain outside this release.
