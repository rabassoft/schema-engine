# Schema Engine 0.1.0 local candidate notes

- **Candidate date:** 2026-07-14
- **Publication state:** Unpublished; local verification only
- **API state:** Public + Experimental + Active
- **Core candidate:** `@rabassoft/schema-engine@0.1.0`
- **Angular candidate:** `@rabassoft/schema-engine-angular@0.1.0`

## Included capabilities

- Framework-neutral compilation of the SPEC-001/SPEC-002/SPEC-003 root-object,
  recursive inline-object, primitive-leaf and homogeneous object-collection
  subset with explicit identity policies.
- Immutable strict deep operations and controlled runtime state with object and
  leaf snapshots, scopes, presence, dirty and validation projection.
- Synchronous external validation, interaction state, scopes, localization and
  immutable snapshots/subscriptions.
- Stable collection/item/node reads and controlled item-leaf set/remove plus
  application-supplied insert, remove and move intentions.
- Angular 22 headless adapter with automatic recursive object/leaf projection,
  fixed stable-keyed collection/item projection, localized semantic groups,
  canonical IDs, scored leaf-renderer resolution and inline `ViewContainerRef`
  instantiation.
- Accessible native string, number/integer, boolean and string-enum renderers
  using private Angular Signal Forms buffers.
- Controlled explicit field clearing that distinguishes missing from `""`,
  `0`, negative zero and `false`.

These outcomes correspond to completed M1–M10 and G0 under SPEC-001 v0.1.15,
SPEC-002 v0.1.2 and SPEC-003 v0.1.2. All seven PLAN-010 checkpoints are
complete; checkpoint 7 repeated the complete M10 review and verification matrix
with zero findings.

## Compatibility evidence

| Adapter | Core     | Declared Angular   | Build Angular | Lower tested | Upper stable tested |
| ------- | -------- | ------------------ | ------------- | ------------ | ------------------- |
| `0.1.x` | `^0.1.0` | `>=22.0.6 <23.0.0` | `22.0.6`      | `22.0.6`     | `22.0.6`            |

- Upper endpoint resolved: 2026-07-14.
- Source: public npm package metadata for `@angular/core` and aligned Angular
  packages.
- Prerelease and deprecated versions are excluded.
- `@angular/core`, `@angular/forms`, and supporting Angular packages are tested
  as an exactly aligned tuple.
- The lower and upper endpoints currently coincide; both matrix checks still
  exercise isolated tarball consumers.

## Stability and migration policy

All root exports remain Experimental. During `0.y`, an incompatible
Experimental API change requires at least a MINOR release, an approved SPEC,
ADR, or plan, and migration notes when repository consumers exist. Neither
`0.1.0` nor packaging promotes an API to Stable.

Only explicit package root entry points are supported. Deep imports are not
part of the public API.

The M9 Experimental migration changes canonical definition/runtime keys from
root field names to serialized deep paths, extends operations and lookup APIs
to string-only deep paths, and replaces repository consumers' manual flat-field
projection with automatic `SchemaFormDirective` object/leaf projection. These
source-level changes remain governed by the `0.y` Experimental policy.

The M10 Experimental migration adds array definitions, item templates, stable
collection addresses, five collection operation variants, positional read
paths and fixed Angular collection/item projection. Numeric paths remain
observation-only; applications continue to own values, identity and controlled
confirmation.

## Known exclusions

Arrays of primitives, arrays inside collection item templates, tuples, JSON
Schema composition and the accepted but unimplemented SPEC-004 `$defs`/`$ref`
subset, generated/editable identity, async
validation, persistence, advanced layout, custom collection renderers, theming,
plugins, undo/redo, publication automation, and all other deferred capabilities
remain excluded.

## Distribution boundary

These tarballs remain `private: true`, have no selected license or distribution
terms, and are not authorized for external distribution. No registry write,
publication, Git tag, GitHub Release, provenance operation, credential use, or
API stability promotion occurred. D-040 and D-034 retain those decisions.
