# Schema Engine M23 stage-only metadata release notes

- **Current state:** core/base Angular `0.4.1` and pilot `0.2.1` are public
  exactly and under `next`, with exact bytes, signatures, provenance and
  consumers verified;
  deterministic clean protected-`main` candidates selected;
  review 189's three byte-mismatched stages have been separately rejected;
  replacement run `30377052519` succeeded and review 196 verifies all three
  replacement stages byte-identical to the selected candidates;
  review 190 validates the deterministic-gzip correction and review 191
  validates its clean rebuild from protected `develop`; PR #19 delivers that
  evidence to `develop@e99193b` and review 192 cycle 6 accepts promotion
  readiness. PR #20 delivers that review as `develop@84d72f9`; review 193
  reconciles the durable promotion gate. PRs #21–#23 deliver, promote and
  reconcile that state; review 194 cycle 3 selects the corrected bytes from
  exact protected `main@028a98c`; review 195 cycle 2 accepts the complete
  replacement pre-dispatch gate with zero findings
- **Date:** 2026-07-30
- **Plan state:** PLAN-025 revision 0 and M23 completed after review 208 cycle 4
  repeated the complete closure with zero findings; pilot `latest` resolves
  `0.2.1`, core/base `latest` resolve `0.4.1` and all three packages use the
  stronger publishing policy
- **Intended channel:** `next`
- **License:** `AGPL-3.0-only` or a separate paid commercial agreement
- **API state:** Public + Experimental + Active; no Stable API or support SLA
- **Repository/provenance:** all three provenance records are verified against
  the public repository, protected `main@028a98c`, workflow
  `npm-publish.yml` and run `30377052519`

## Exact proposed line

| Package                                 | Version | Preserved packed Schema Engine peer |
| --------------------------------------- | ------- | ----------------------------------- |
| `@rabassoft/schema-engine`              | `0.4.1` | none                                |
| `@rabassoft/schema-engine-angular`      | `0.4.1` | core `^0.4.0`                       |
| `@rabassoft/schema-engine-angular-aria` | `0.2.1` | base Angular `^0.4.0`               |

These independent PATCH versions change distribution metadata and release
security only. They add no runtime behavior, declaration, export, dependency,
diagnostic, framework-range or support-tier change. Equal core/base versions do
not establish lockstep.

Source peers use explicit `workspace:^0.4.0`; packed artifacts must contain
ordinary `^0.4.0`, never `workspace:` or narrowed `^0.4.1`. Angular core/forms
remain `>=22.0.6 <23.0.0`; Angular Aria/CDK remain
`>=22.0.5 <23.0.0`, with Angular core `>=22.0.6 <23.0.0`.

## Selected protected-main candidate evidence

Checkpoint 3 first generated the original `.release/0.4.1` candidates. Review
189 later proved that their platform-native gzip bytes differed from the Linux
stages while TAR/content remained identical. Reviews 190–191 corrected the gzip
wrapper and reproduced the normalized bytes locally and from protected
`develop`. PRs #21–#23 then delivered, promoted and reconciled the correction.
Review 194 cycle 3 rebuilds twice from exact protected
`main@028a98cfb1c96c821b6233c82f688a416e987656` under Node `22.23.1`, npm
`11.18.0` and pnpm `10.28.2`. Current selected evidence records
`sourceCommit: 028a98cfb1c96c821b6233c82f688a416e987656`; both generations are
byte-identical to each other and to reviews 190–191's corrected values.

| Package                                       | Bytes   | SHA-512 (hex)                                                                                                                      |
| --------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `@rabassoft/schema-engine@0.4.1`              | 217,599 | `7a2f641fbf6bea6f217987bd67883fa5e7654fd60f32db51b1d73998cb8e09f7885cf9bb5a02e046138d9d83558038b173490229a51da89bb6ea11bddffb6592` |
| `@rabassoft/schema-engine-angular@0.4.1`      | 127,734 | `016138d763fcee7e80eebb3a0c1f05e39d96efea94a07ada4a48f1c16e3550b27531ed8f70da3e3b51627f3a7fd89c98afcf6ec5ad0889d7ddde4e59024f961a` |
| `@rabassoft/schema-engine-angular-aria@0.2.1` | 29,281  | `6f3607c4bae84a933763f16e8edff7c7e22347151b2fb5776d75e0bd385ad5a1f955b505ddd0a04800c82f527378277082bdabd9ff3f00d54fb26465a7288856` |

Their exact integrity values are:

- core:
  `sha512-ei9kH79r6m8heYe9Z4g/pedlT9YPMttRsdc5mMuOCfeIXPm7WgLgRhONnYNVgDixc0kCKaUdqJu26hG93/tlkg==`;
- base Angular:
  `sha512-AWE412P87n6A7rs6DB8F452W7+qUoHraSkjxwW41ULJ1Me2PcNo+O1Fifzp/2JyYr89uxa0Iidfd3k5ZAk+WGg==`; and
- Angular Aria pilot:
  `sha512-bzYHxLroSpM3Y/Fujt/3x+IjRxUbL7V3bXXgvTha1aH5VbUF3dCgSADIL1JzeCdwgr2r2f8/ANVPsmRlpyiIVg==`.

All non-metadata bytes and inventories are identical to the selected M21
candidates. Only `README.md` and the reviewed `package.json` fields differ.
Corresponding Source rebuilds, lower/current native and pilot consumers,
SPEC-009 behavior, M18 regressions, security/rights checks and neutral-path
credential-free dry runs pass.

These protected-`main` candidates are selected publishable evidence. All three
packages are now published and verified from that evidence. Publication does
not imply an alias transition; every remaining npm action stays separately
gated.

## Deterministic-gzip recovery

Review 190 cycle 3 validates a local correction that decompresses the
`pnpm pack` result and recompresses its unchanged TAR with pinned
`fflate@0.8.3`, level 9, `mtime=0` and gzip OS byte `3`. Two independent local
generations are byte-identical, and normalizing copies of the three selected
protected-main candidates produces those same bytes:

| Package                                       | Comparison bytes | Comparison SHA-512 (hex)                                                                                                           |
| --------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `@rabassoft/schema-engine@0.4.1`              | 217,599          | `7a2f641fbf6bea6f217987bd67883fa5e7654fd60f32db51b1d73998cb8e09f7885cf9bb5a02e046138d9d83558038b173490229a51da89bb6ea11bddffb6592` |
| `@rabassoft/schema-engine-angular@0.4.1`      | 127,734          | `016138d763fcee7e80eebb3a0c1f05e39d96efea94a07ada4a48f1c16e3550b27531ed8f70da3e3b51627f3a7fd89c98afcf6ec5ad0889d7ddde4e59024f961a` |
| `@rabassoft/schema-engine-angular-aria@0.2.1` | 29,281           | `6f3607c4bae84a933763f16e8edff7c7e22347151b2fb5776d75e0bd385ad5a1f955b505ddd0a04800c82f527378277082bdabd9ff3f00d54fb26465a7288856` |

PR #18 merged the correction as protected
`develop@5e60796ac694f1d610e449683284707e1774571b`. Review 191 cycle 2 repeats
two complete clean generations and obtains these exact same bytes, now with
candidate evidence recording that exact `sourceCommit`; neutral dry-runs,
three-package Corresponding Source rebuilds and security/rights checks pass.
PR #19 then delivered review 191 evidence as protected
`develop@e99193b2ec71788c4bbc1149a4056fbf4d74747c`; its required and
post-merge CI passed. Review 192 cycle 6 accepts the protected-main promotion
payload after its documentation-only evidence reaches `develop` and the exact
head is reobserved; it performs no promotion.
PR #20 delivered that evidence as protected
`develop@84d72f938d66ff5e78f88f610826b418c8d3dc11`; required and post-merge CI
passed. Review 193 cycle 2 reconciles the post-delivery next gate without
selecting protected-main candidates. PR #21 delivers that reconciliation as
`develop@ed1cd2d`; PR #22 promotes it as protected `main@028a98c`, and PR #23
reconciles it as `develop@0933924`. All required and post-merge CI gates pass.
Review 194 cycle 3 then reproduces these values in two exact protected-`main`
generations and selects them as publishable evidence.

The MIT dependency is development-only and is absent from every published
package dependency graph and tarball.

## Staged-state model

One separately authorized GitHub workflow may then stage the selected tarballs
under `next` in dependency order:

1. core `0.4.1`;
2. base Angular `0.4.1`; and
3. Angular Aria pilot `0.2.1`.

Staging reserves an immutable version but does not make it public. Every stage
must be listed/viewed, downloaded and compared byte-for-byte with the selected
candidate before approval. A pending stage supplies no installation or live
provenance claim.

## Approval and live-state model

Each package requires a separate Ricard 2FA approval in dependency order. After
each approval, verification must observe exact bytes/integrity, registry
signature, public access, repository metadata and automatic provenance bound to
`rabassoft/schema-engine`, `.github/workflows/npm-publish.yml` and the exact
protected source commit.

Partial publication is truthful but incomplete:

- core only leaves base/pilot unpublished and defaults unchanged;
- core/base leaves the pilot unpublished and coordinated completion unclaimed;
- all three exact/`next` versions must pass before any `latest` transition.

After all three exact/`next` lines pass, `latest` may move
deepest-dependent-first: pilot, base Angular, then core. The first two states are
planned mixed windows and provide no accepted coordinated `latest` or
unqualified evidence. Only the final verified alias and consumer matrix may
close M23.

## Installation

Consumers may install the exact verified M23 line:

```sh
npm install @rabassoft/schema-engine@0.4.1 @rabassoft/schema-engine-angular@0.4.1 @rabassoft/schema-engine-angular-aria@0.2.1
```

The coordinated `next` channel resolves the same line:

```sh
npm install @rabassoft/schema-engine@next @rabassoft/schema-engine-angular@next @rabassoft/schema-engine-angular-aria@next
```

The exact, `next`, `latest` and unqualified routes now resolve the coordinated
M23 line. Eight lower/current native/pilot consumer invocations verify all four
routes. Registry routing remains Experimental and does not imply Stable.

## Trusted-publication boundary

Each package receives one exact trusted publisher:

- GitHub Actions repository `rabassoft/schema-engine`;
- workflow filename `npm-publish.yml`;
- protected environment `npm-publish`; and
- `npm stage publish` only, with direct `npm publish` disabled.

The workflow uses Node `22.23.1`, exact npm `11.18.0`, pnpm `10.28.2`,
GitHub-hosted execution, no cache or stored release credential, and
`id-token: write` only in the protected staging job. It stages only the three
descriptor-named tarballs. Current official minimums must be reobserved before
external use.

Trusted publishing automatically generates provenance for a public package
from a public supported GitHub-hosted workflow. Source preparation does not:
the registry attestation, signature and source binding remain mandatory live
evidence.

## Immutable recovery

- Before staging, stop without registry mutation.
- Preserve valid pending stages; reject one only after separate authorization.
- Never reuse a rejected or reserved version silently.
- Preserve every approved immutable version; a defect requires deprecation
  and/or a later reviewed PATCH, never overwrite or assumed unpublish.
- Preserve mixed aliases or apply only a separately authorized corrective tag.
- Never fall back from OIDC to a stored publish token.
- Missing/mismatched provenance blocks dependent approval and every alias move.
- Restrict traditional tokens only after the complete trusted cycle passes.

M23 does not authorize another package/framework, Stable promotion, wider peer,
Git tag, GitHub Release or deletion of private recovery material.
